/**
 * Faire un programme qui écrit les chiffres de 1 à 10 à l'écran
 * en utilisant une boucle `for`
 *
 * Chaque chiffre séparé par un retour à la ligne `\n`
 */
#include <stdio.h>

int main(void) {
    for(int i = 1; i <= 10; i++) {
        printf("%d\n", i);
    }
}
