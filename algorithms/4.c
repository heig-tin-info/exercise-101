/**
 * Afficher un menu
 */

#include <stdio.h>

int main(void)
{
    printf("Menu\n----\n");
    printf("1. Afficher distance terre lune\n");
    printf("2. Afficher la réponse à la grande question\n");
    printf("3. Le nom de l'élu\n");
    printf("4. Le nombre de pommes dans une tarte aux pommes\n");
    printf("0. Quitter le programme");

    // Ici vous pouvez utiliser `if` et `else if`
    // Ainsi qu'une boucle...

    int choice = 0;
    printf("Votre choix [%d]: ", choice);

}
