/**
 * Completez le programme avec les bons opérateures
 */

int main(void)
{
    printf("1. A ET(bit-à-bit) B donne %d", );
    printf("2. A OU(bitwise) B donne %d", );

    printf("3. Le reste de A divisé par B donne", );
    printf("4. La division entière de A par B donne", );

    printf("5. La valeur de A décalé à droite de 3", );
    printf("6. La valeur de A décalé à gauche de 2", );

    printf("7. A XOR B", )

    printf("8. La valeur du troisième bit de A");

    printf("9. La valeur du A modulo 8 ième bit de B", );

}
